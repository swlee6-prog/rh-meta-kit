# 마케터 핸드오프 2026-09-17 (swlee6)

## 지금 할 일 (2개)

1. **CLI 1회 갱신.** 작업대 agents 탭 → "파일을 개별로 다운로드" → CLI 파일을 받아 기존 `rh-meta.mjs`에 덮어쓰기.
   이후에는 Claude가 `doctor`에서 구버전을 감지하면 `update` 한 번으로 알아서 갱신합니다. (이 폴더 파일이 이미 최신이라 그대로 써도 됩니다.)
2. **v01 재시도 1회.** Claude Code에서 이 폴더를 열고 아래 명령을 그대로 실행해 달라고 하세요.
   `node rh-meta.mjs submit 0d154305-09bb-4d4c-9bd0-eb96bd27fb95 --confirm-new-paused --supersede-failed 6829d49a-810e-421a-a6d7-1adee8f384df`
   성공하면 새 PAUSED 광고 1건이 생기고 작업 상태가 마케터 검수 대기가 됩니다.

**기한: 9/19.** 초안 임시 파일이 생성 후 2일이면 만료됩니다. 만료되면 같은 영상 3개로 새 초안을 만들어 같은 명령(초안 번호만 교체)으로 하면 됩니다.

## 무슨 일이 있었는지

- 9/15~16: v01~v05 제출 5건이 Meta creative 검증 단계에서 실패했습니다. 원인은 Meta가 폐지한 옵션(`standard_enhancements`)을 보내서 생긴 code 100 거절이었습니다. 제거 후 검증 통과 확인했습니다.
- 9/16~17: 실패한 작업이 예약 번호를 영원히 물고 있어서 같은 이름·영상으로 재시도가 막히는 문제를 발견했습니다. 이제 본인 실패 작업은 명시적 대체 제출(`--supersede-failed`)로 재시도할 수 있습니다. 실패 이력은 그대로 남고, 새 작업이 예약을 이어받습니다.
- 같은 기간: 이력 검색 500 오류(긴 이름), 브라우저 중복 제출 오안내, 중단 작업 수습 불가, 알 수 없는(UNKNOWN) 실패의 키 점유를 모두 수정하고 배포했습니다.

## 막히면

- `403 본인 작업만` → 실패 작업과 초안을 만든 본인 계정(swlee6)으로 로그인했는지 확인하세요.
- `409 대체 불가` → Claude에게 `status`와 증빙을 받아서 전달해 주세요. creative·광고가 이미 생겼거나 생겼을 수 있는 작업은 자동 재시도하지 않습니다.
- `409 초안 만료`/`410` → 같은 영상으로 새 초안부터 다시 만들면 됩니다.
- 브라우저에서 "이미 제출된 작업"이 뜨면 → 새로 제출된 게 아니라 기존 작업을 열어준 것입니다. 화면의 작업 번호를 확인하세요.
